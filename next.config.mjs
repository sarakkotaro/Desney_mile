/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  images: {
    domains: ["nquqzhtbapbnjkyudrwl.supabase.co"],
  },
};

export default nextConfig;
